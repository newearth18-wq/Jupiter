# Jupiter Mission runtime

SET 4 durable Mission Manager with an explicit finite-state machine, normalized persistence, linked execution attempts, safe-boundary pause, propagated cancellation, verification guards, and reconstructed human-readable timelines.

This package does not plan or execute workflows. Planner and Workflow Engine behavior remains unavailable until its owning SET.
